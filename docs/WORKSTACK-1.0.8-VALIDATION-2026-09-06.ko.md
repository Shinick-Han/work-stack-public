# Work Stack 1.0.8 설치 및 검증 기록

2026-09-06. 배포 소스: `a7ec29d79495eafe845e214dcef1942d9199c133`.

결함 수정본을 통합하여 Windows 설치와 EXE 교체를 완료했다. 기존 사용 작업 공간의 식별자와 경로를 유지했고, 설치 후 서버의 `ready` 응답을 확인했다. 원격 배포 브랜치 `codex/o1-1.0.7-candidate-20260904`에 같은 소스를 fast-forward push했다. 브랜치 이름은 기존 전달 경로를 유지한 것이며 실제 제품 버전은 **1.0.8**, EXE 파일 버전은 **1.0.8.0**이다.

## 검증 범위

| 항목 | 결과와 정확한 범위 |
|---|---|
| 전체 백엔드 | `c175939`에서 3,131개 실행, 실패 0, 환경 관련 skip 11. 네 개의 겹치지 않는 패턴으로 전체 테스트 모듈을 실행했다. |
| 별도 Agent Oracle | 같은 후보에서 111개 통과, skip 0. |
| 프런트엔드 단위 검사 | 1,532개 통과. 이후 프런트엔드 소스 변경 없음. |
| 실제 브라우저 | 61개 통과. 데스크톱·좁은 화면의 Board/Graph/Table 및 보고서 화면 포함. |
| 구조 검사 | 310개 제품 파일 통과. 순환 의존성 해소, 새 ZIP helper 등록, 부채 한도 완화 없음. |
| 마지막 사전 백업 수정 | `a7ec29d` 독립 검수 PASS. 유지보수 35개 실행/skip 1, CLI characterization 13개 통과. 실제 구형 작업 공간의 새 CLI 백업도 이전 런타임의 백업과 9개 문서 바이트가 같았다. |
| 실제 Linux 설치 시험 | `202bad2` 배포물과 CPython 3.12.14에서 설치·probe·기존 설치 거절·테스트 데이터 9개 파일 보존 PASS. 회사 SSH 또는 원격 전송 전체를 검증한 것은 아니다. |
| 설치 파일 검사 | 설치 파일 checksum 검증 및 패키지의 내장 프로필 시나리오 검사 통과. |
| 설치본 소스 | 제품 Python 100개, desktop host Python 29개, 프런트 배포물 14개가 배포 소스와 동일함을 확인했다. |

전체 백엔드 이후의 변경은 동작이 같은 말미 공백 정리와 오프라인 사전 백업 경로 보완이다. 마지막 백업 변경은 위의 영향 범위 검사·독립 검수·실제 설치로 검증했다. 전체 검사가 최종 커밋에서 재실행됐다고 주장하지 않는다.

엄격한 단일 환경 G30의 전체 PASS 영수증은 아직 없다. Windows에서는 심볼릭 링크 권한 때문에 중단됐고, Linux에서는 Python/Node 의존성 부족으로 끝까지 실행되지 못했다. Linux CPython 3.12에서는 권한 때문에 건너뛴 authority 검사를 27/27 통과했다. 이 환경 한계를 시험이나 runner의 완화로 숨기지 않았다.

## 데이터와 복구

- 교체 전 기존 1.0.7 런타임으로 실제 작업 공간과 별도 데모 공간을 각각 백업하고 검증했다. 구형 설치 파일과 checksum도 보존했다.
- 실제 작업 공간은 첫 실행 시 v3에서 v5로 이행했다. 기존 의미 문서 8개는 내용이 같고, metadata의 정확한 이행 기록과 기본 `reports.json`만 추가됨을 확인했다.
- 작업 공간 내부의 자동 이행 전 ZIP도 기존 백업과 같은 내용을 갖는지 검증했다. 별도 데모 데이터는 v3와 기존 바이트를 유지했다.
- v5 데이터에 구형 EXE만 덮어씌우는 방식은 복구 절차가 아니다. 구형 런타임과 검증된 v3 백업을 함께 보존한다.

설치 파일 SHA-256: `9299b4d349f43615df791cc66095a75ba3821bf7f025a30a56c2975e37b08a65`.

설치 EXE SHA-256: `e43ccb40ee12efd17af774a86811dfb93704da0c0bf762b8890df848d030610c`.

## 다음 작업

1. **병렬 진행:** 보고서 저장 서비스를 실제 WorkStack에 연결(Claude), 원격 수집기·설치기의 v3/v5 메타데이터 인식(Cursor).
2. 각각 독립 검수 후 보고서 HTTP API와 원격 실행기의 전송·결과 불명 처리 구현을 진행한다.
3. 검수된 API/호스트 계약 위에 클라이언트와 UI를 연결한다. 인터페이스가 정해진 화면끼리는 병렬 진행한다.

보고서 저장 UI, 원격 설치 UI, 회사 환경의 실제 설치까지 완료한 상태는 아니다. Task T-0016/T-0039에는 배포 성과와 다음 단계를 체크포인트로 기록했고 Task 자체를 완료 처리하지 않았다.
