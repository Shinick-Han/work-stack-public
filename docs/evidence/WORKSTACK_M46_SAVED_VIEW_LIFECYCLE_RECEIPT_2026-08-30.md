# Work Stack M46 saved-view lifecycle receipt

Date: 2026-08-30

## Product coordinate

- Branch: `codex/workstack-ui-actions-20260830`
- Product commit: `59d0a4d3d2995ab744c2a3e6df7f41ba2b25b4ed`
- Product tree: `7292e9573dca9e7c108b6b0df9f3563bedd918ac`
- Previous checkpoint was pushed before implementation; this product commit was not yet pushed
  when the receipt was written.

## Delivered behavior

- Save view opens an explicit bounded name editor instead of silently accepting an autogenerated
  name.
- Applying a view retains its identity while the user changes filters or the workspace surface.
- Update writes the current search, status, priority, readiness, due timing, Objective, and view
  coordinates to that exact ID while preserving its name.
- Rename changes only the selected view name; Remove deletes only the selected ID.
- Existing strict parsing, the 12-view browser-local bound, and safe readiness/timing defaults are
  preserved.
- WorkspacePage is lazy-loaded as a cohesive user-visible surface, lowering initial JS while
  keeping the app shell available.

## Verification

- RED-first tests failed because Rename and the naming editor did not exist.
- Full frontend gate: 34 files / 173 tests passed.
- Playwright: the existing saved-view scenario initially failed at the new explicit naming step;
  after updating the scenario, the exact flow passed in isolation. The most recent complete run had
  21 other scenarios green and only that expected stale-test failure before correction.
- Production build passed without a chunk-size warning: initial JS 438.79 kB, WorkspacePage
  62.45 kB, Table 3.19 kB, Task Drawer 35.59 kB, Graph 173.04 kB, Treemap 241.16 kB.
- Source export audit before this receipt: 255 UTF-8 text files. Expected count after adding this
  receipt: 256.
- `git diff --check` passed.
- Backend behavior was unchanged; its most recent complete gate remains 138 tests passed with one
  explicit Windows symlink privilege skip.
- The original checkout's six protected user-owned dirty paths were not edited or staged.

## Docking and safety boundary

The frozen contract (`cb0a41f250ad1a4bff287a8268cb49e805770cf94682647fbac0e89375102f70`),
safety-policy root (`sha256:48dfd17ec86942e44692a26e7d43b88387c2c97f208c4d1081878a9318440148`),
and conformance-kit root (`sha256:a18627e56e49bf0f488a4e42962faeb559e8d9e99ec4c27e0b389c2a075ae2b9`)
were not changed. Saved views are browser-local preferences and cannot mutate Task revisions,
canonical snapshot serialization, or disclosure behavior.

No Conduit client, loopback transport, watcher, cloud relay, back-sync, bulk import, mutable link
table, execution inference, agent start, or Taskroom start was added.

## Nonclaims and remaining dependencies

- Saved views are local to one browser profile and are not server-shared collaboration state.
- No automatic provider action or planning mutation is inferred from a saved view.
- OS publisher signing and real Microsoft provider Gate 0 evidence remain separate lanes.
- No Conduit consumer implementation was performed.
